-------------------------------------------------
中置関数の宣言。中置記法では :id を使用するので
!define ' :id val と関数を宣言する。
-------------------------------------------------
#[ (a) : * -> * -> * = a ]
!do
  !define ' :!! !lambda x !lambda y !do !print x !print y
  (1 :!! 2)