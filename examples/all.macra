#[ all a : allExpr             -> toplevel = a ]
#[ {a}   : allExpr             -> allExpr  = a ]
#[ a; b  : toplevel -> allExpr -> allExpr  = !lambda p !if p b nil a ]
#[ end   :                        allExpr  = p ]

-----------------------------------

all { a; b; c; end } の形式で使う。
a, b, c を順に評価し、 nil であれば
その時点で nil を返す。
そうでなければ次の式を評価する。

直前の式の戻り値は `p' で参照できる。
-----------------------------------
all { !print 1
    ; !print p
    ; !print nil
    ; !print 2
    ; end
    }