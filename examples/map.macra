(a) : list -> list
    = a;

a => b : expr -> expr -> function
       = !lambda a b;

a, b : expr -> function -> function
     = !lambda a b;

map :: function -> list;
list :: list -> toplevel;

list (map x => !print x);