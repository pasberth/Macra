#[ a; b : toplevel -> toplevel -> toplevel
        = !do a b ]
#[ bar : foo = 1 ]
#[ fuga : hoge = 2 ]

# f :: foo -> hoge -> toplevel

  !define f !lambda x !lambda y 3
; !print !funcall !funcall f bar fuga
; !print !funcall !funcall f fuga bar