#[ a; b      : * -> * -> * = !do a b ]
#[ f :into x : * -> * -> * = f x     ]


----------------------------------------
:into など好きなキーワードをこのように
定義すれば、関数の引数を与えるとき
その好きなキーワードを使える。
----------------------------------------
!define f !lambda x !print x;
!define g !lambda x !lambda y y;
f :into g 1 2